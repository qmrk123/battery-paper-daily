# Core pipeline (Phase 1-2): fetch + build, no API keys needed
requests>=2.31
PyYAML>=6.0
python-dateutil>=2.9

# Phase 3 (graphical-abstract best-effort): image validation
pillow>=10.0

# Phase 4 (Korean summaries): Anthropic Batch API
anthropic>=0.40

# Local-only: graphical abstracts via publisher TDM API (pipeline/tdm_images.py).
# Runs on the campus network, not in CI — but kept here so the shared .venv has it.
pymupdf>=1.24
